# Sample document

This is a paragraph with **bold**, *italic*, `inline code`, a
[markdown link](https://example.com), and a bare URL
https://kernel.org and www.archlinux.org with trailing punct
(https://example.com/path).

## Headings work too

> A blockquote with a link to https://example.org inside.

- list item one
- list item **two**
- item with `code`

1. ordered one
2. ordered two

---

```c
#include <stdio.h>
int main(void) { printf("hello\n"); return 0; }
```

Escaping test: 5 < 10 & 3 > 2.
